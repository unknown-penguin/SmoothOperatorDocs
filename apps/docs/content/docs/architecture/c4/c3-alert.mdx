---
title: c3 - alert
description: Component diagram for the Alert Service.
---

## Alert Service — Components

<Mermaid chart={`
flowchart TD
  subgraph Alert["Alert Service\n(.NET)"]
    KafkaConsumer["Kafka Consumer\n(telemetry.enriched.v1)"]
    RuleEngine["Rule Engine\n(configurable rules, thresholds)"]
    Notifier["Notifier\n(UI push, email, webhook)"]
    RBAC["RBAC Validator\n(Auth integration)"]
    Health["Health & Metrics"]
  end

  Kafka[("Kafka Cluster\ninfra")]
  UI["Frontend\n(React)"]
  Auth["Auth Service\n(.NET)"]

  Kafka --> KafkaConsumer
  KafkaConsumer --> RuleEngine
  RuleEngine -->|validate| RBAC
  RuleEngine --> Notifier
  Notifier --> UI
  RBAC --> Auth

  class Alert dotnet;
  class Kafka infra;
  classDef dotnet fill:#8b5cf6,stroke:#5b21b6,color:#fff;
  classDef infra fill:#374151,stroke:#111827,color:#ffffff;
  `} />

Notes: rule definitions stored separately; supports external webhook and internal UI notifications.
