---
title: c3 - monitoring
description: Component diagram for the Car Monitoring (Query API).
---

## Car Monitoring Service — Components

<Mermaid chart={`
flowchart TD
  subgraph Mon["Car Monitoring Service\n(.NET)"]
    KafkaConsumer["Kafka Consumer\n(telemetry.enriched.v1)"]
    ReadModelBuilder["Read-Model Builder\n(aggregate, materialize)"]
    QueryAPI["Query API\n(HTTP/JSON)"]
    Cache["Cache Adapter\n(Redis)"]
    Metrics["Health & Metrics"]
  end

  Kafka[("Kafka Cluster\ninfra")]
  TS[("TimescaleDB\n(Postgres)")]
  Redis[("Redis Cache\ninfra")]
  UI["Frontend\n(React)"]

  Kafka --> KafkaConsumer
  KafkaConsumer --> ReadModelBuilder
  ReadModelBuilder -->|persist| TS
  ReadModelBuilder -->|cache| Cache
  QueryAPI -->|query| TS
  QueryAPI -->|fast reads| Cache
  QueryAPI --> UI

  class Mon dotnet;
  class Kafka,TS,Redis infra;
  classDef dotnet fill:#8b5cf6,stroke:#5b21b6,color:#fff;
  classDef infra fill:#374151,stroke:#111827,color:#ffffff;
  `} />

Notes: read-optimized, materializes compact DTOs for the UI and realtime components.
