---
title: c1
description: View the Scalar Galaxy example OpenAPI schema.
---

<Diagram lang="mermaid" chart={`
flowchart TD
  %% C1 - System Context (simplified labels to avoid parsing issues)
  actorCar["Race Car (on-car telemetry)"]
  actorSim["ECU Telemetry Simulator"]
  actorRaceProvider["Race Provider (flags, weather, position)"]
  actorUser["User (Race Engineer / Race Director)"]
  actorCI["CI / Monitoring / Alerting Systems"]

  subgraph System["Telemetry Platform"]
    Platform["Telemetry Platform\n(real-time + monitoring + APIs)"]
  end

  actorCar -->|"MQTT: telemetry/*"| Platform
  actorSim -->|"MQTT: telemetry/*"| Platform
  actorRaceProvider -->|"REST/gRPC (flags, weather, position)"| Platform
  actorUser -->|"web UI / commands / monitoring"| Platform
  actorCI -->|"alerts / metrics / API queries"| Platform

  classDef external fill:#6b7280,stroke:#374151;
  class actorCar,actorSim,actorRaceProvider,actorUser,actorCI external;
  class System fill:#374151,stroke:#111827;
`} />